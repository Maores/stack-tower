# Run evidence and reference material stay local (reference stills are
# App Store marketing images and must not be redistributed).
reference/
reference-vetting/
review/

# Project instructions stay local.
CLAUDE.md

# Subagent-driven-development scratch (progress ledger).
.superpowers/

# Playwright MCP session artifacts.
.playwright-mcp/

# Raw Freesound downloads (the used cuts are embedded in audio.js).
fart_sounds_freesound/
